# 日语/韩语属性补齐作业规范（ja/ko 批处理）

站点：`C:\Users\micro\taige-website`（GitHub Pages 多语言静态站 taigetag.com），`data-*` 属性驱动，
`gen_i18n.py <lang> <file...>` 依据属性生成 `/en /fr /es /ja /ko` 目录的静态页。
本作业：给**博客文章**（`blog/*.html`）补齐日语 `data-ja` 与韩语 `data-ko`。

## 环境
- Windows + git-bash。python 必须用 `"$HOME/.local/bin/python3"`（裸 `python` 是 WindowsApps 假 shim，会静默失败）。
- 先 `export PYTHONIOENCODING=utf-8`。文件均为 UTF-8（多数 CRLF），写入时保持原换行。
- 不要 `git commit` / `git push`。

## 任务定义
对每个文件：给**所有带 `data-zh` 属性的元素**补两个属性 `data-ja`、`data-ko`，值为该中文的**真实翻译**
（同元素的 `data-en` 是最可靠的语义参考）。
范围是整页：正文（`<section class="article-body">`）**以及**导航、面包屑、侧栏、相关文章卡片、CTA、页脚、
`<title>`、`<meta name="description">`——凡带 `data-zh` 的都要补。

## 硬规则
1. 插入位置：**紧跟 `data-en="…"` 之后**；该元素没有 `data-en` 时紧跟 `data-zh` 之后。
2. 属性值用双引号包裹，**值内部严禁裸双引号**（日语用「」，韩语用 `&quot;` 或不使用引号）。
   裸双引号是移动端 WebKit 白屏崩溃的元凶。
3. 保留元素内部 `<strong>`/`<em>`/`<br>`/`<a>` 等标签及其位置；`&` 写 `&amp;`；emoji 保留；
   数字、邮箱、电话、尺寸单位、百分比不改（只翻译文字）。
4. **不要修改**：已有 `data-zh/en/fr/es` 的值、`class`/`id`/`href`、`<script>` 里的 JSON-LD、
   语言切换按钮（`data-lang=…`）、缩进与整体格式。
5. 只**增加属性**，不改元素内的默认中文文本（默认文本保持中文）。
6. 用精确正则/替换处理，**不要整体重写或重新格式化**整个 HTML。
7. 博客页的 `<meta name="description">` 是**多行属性**（data-zh 一行、data-fr/data-es/data-en 各自一行、content 最后）。
   只在 `data-en=` 那一行后追加 `data-ja="…"` 与 `data-ko="…"` 两行，保持多行结构与缩进。

## 术语表（必须与主站一致，逐字使用）
| 中文 | 日语 | 韩语 |
|---|---|---|
| 吊牌 / 服装吊牌 | タグ / 衣料用タグ | 행택 |
| 织唛 / 主唛 | 織りラベル / メインラベル | 직조 라벨 / 메인 라벨 |
| 洗水标 | 洗濯表示ラベル | 세탁 표시 라벨 |
| 包装袋 | 包装袋 | 포장백 |
| 环保纸袋 | エコ紙袋 | 친환경 종이백 |
| 宣传手册 | パンフレット | 브로슈어 |
| 辅料 / 服装辅料 | 副資材 / 衣料副資材 | 부자재 / 의류 부자재 |
| 辅料 FAQ | 副資材 FAQ | 부자재 FAQ |
| 起订量 / MOQ | 最小ロット MOQ | 최소 주문량 MOQ |
| 打样 / 出样 | サンプル作成 | 샘플 제작 |
| 报价 / 询价 | お見積り | 견적 |
| 交期 / 货期 | 納期 | 납기 |
| 质检 / 品控 | 品質検査 / 品質管理 | 품질 검사 / 품질 관리 |
| 法规合规 | コンプライアンス / 法規対応 | 규정 준수 |

- 公司名：日语 `東莞泰閣包装製品有限公司`；韩语 `둥관 TAGE 패키징 유한공사`。
- 城市：日语 `東莞`；韩语 **`둥관`**（严禁写成 `동관`）；虎门 = 日语 `虎門` / 韩语 `후먼`。
- 风格：日语商务敬体（です・ます調），韩语正式商务体（-습니다/-입니다체）。

## 收尾自检（必做，并把输出贴进报告）
```bash
cd /c/Users/micro/taige-website
export PYTHONIOENCODING=utf-8
"$HOME/.local/bin/python3" ja_ko_translate_helper.py verify <你负责的每个文件，空格分隔>
```
要求：每个文件 `zh == ja == ko` 且 `引号异常=0`（脚本用引号感知扫描器，比简单 `[^>]*` 正则可靠）。
若某文件不达标，继续修到达标为止。

## 血泪坑（第一批实测，必须避免）

1. **插入偏移的坐标系 bug（会静默损坏文件）**：用正则匹配到标签后，属性搜索跑在 `m.group(2)` 上、切片却按 `m.group(0)` 的偏移 → 插入点整体左移 4 字符，把属性值切成两半（实测把 `spot UV` 切成 `spot…UV`）。
   而「zh==ja==ko 计数相等 + 引号异常=0」这类校验**全部照常通过**，属静默损坏。
   → 正确做法：在**同一段字符串**（整段属性或整行）上算偏移与切片；插入后必须验证 `(tag, data-zh, data-en)` 序列与编辑前快照逐元素一致。
2. **值内引号**：`data-ja/data-ko` 值内不得出现裸 `"` 或 `'`。含链接的译文用 `&quot;` / `&#39;`（`gen_i18n.py` 生成页时会 `html.unescape`，浏览器渲染仍是合法的 `<a href="…">`）。
3. **换行风格**：先探测文件是 LF 还是 CRLF，原样保持；写回时用 `newline=''` 避免被自动改成 CRLF。
4. **临时文件不要丢在仓库根目录**（本站是 GitHub Pages，仓库根文件会被发布）。放 `%TEMP%` 或仓库外目录，收尾清理。
5. 不要 `git commit` / `git push`，不要运行 `gen_i18n.py`（会与其他并行子代理写同一目录冲突）。

## 报告格式
1. 每个文件的 verify 输出行
2. 3 条翻译样例（中文 → 日语 / 韩语）
3. 如遇任何跳过或异常，明确说明文件与原因
